import { IFloatingAction } from "./types";

const base: IFloatingAction = {
  text: "2 NFTs selected -",
  buttonText: "Move to Azuki",
  onClickAction: () => {},
};

export const mockFloatingActionProps = {
  base,
};
