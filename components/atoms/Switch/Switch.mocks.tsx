import { ISwitch } from "./types";

const primary: ISwitch = {
  id: "id",
  label: "Label",
  type: "primary",
};

const secondary: ISwitch = {
  id: "id",
  label: "Label",
  type: "secondary",
};

export const mockSwitchProps = {
  primary,
  secondary,
};